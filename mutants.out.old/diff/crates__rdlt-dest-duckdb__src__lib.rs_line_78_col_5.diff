--- crates/rdlt-dest-duckdb/src/lib.rs
+++ replace fatal -> DestError with Default::default()
@@ -70,17 +70,17 @@
     pub fn query_string(&self, sql: &str) -> Result<String, DestError> {
         let conn = self.clone_conn()?;
         conn.query_row(sql, [], |row| row.get::<_, String>(0))
             .map_err(fatal)
     }
 }
 
 fn fatal(e: impl std::fmt::Display) -> DestError {
-    DestError::fatal(e.to_string())
+    Default::default() /* ~ changed by cargo-mutants ~ */
 }
 
 /// Crash-point injection (feature 003 R20). No-op without the `failpoints`
 /// feature. Coarse by design: DuckDB's own transaction is one atomic step.
 macro_rules! crash_point {
     ($name:expr) => {
         #[cfg(feature = "failpoints")]
         fail::fail_point!($name, |_| {
