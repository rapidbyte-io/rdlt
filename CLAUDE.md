<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:
`specs/003-hardening-performance/plan.md` (feature: hardening & performance —
crash-point sweep, mutation/fuzz/property nets, streaming shred path, remaining
benchmark cells, blocking perf gate; spec, research, data model, and quality-gate
contract live alongside it). The established architecture is feature 001:
`specs/001-rdlt-ingestion-engine/plan.md` and its contracts (as amended by
feature 002) remain authoritative; the approved technical design is
`2026-07-18-rdlt-engine-design.md` at the repo root. Run tests with
`cargo nextest run` (doc-tests: `cargo test --doc`).
<!-- SPECKIT END -->
