pipeline: strat-duck-delete-insert
workdir: {{workdir}}/.rdlt
write_mode: {merge: {key: [id]}}
source:
  postgres:
    conn: "{{conn}}"
    tables:
      - name: pg_wide
destination:
  duckdb:
    path: {{workdir}}/strat.duckdb
    merge_strategy: delete_insert
