//! One compile root, mirroring the house layout: topic files under
//! `cases/`. This crate's other net is `src/handshake.rs`'s own inline
//! unit tests (the line format is pure logic with no build-time
//! dependency, so it stays with the code it tests).

mod cases;
