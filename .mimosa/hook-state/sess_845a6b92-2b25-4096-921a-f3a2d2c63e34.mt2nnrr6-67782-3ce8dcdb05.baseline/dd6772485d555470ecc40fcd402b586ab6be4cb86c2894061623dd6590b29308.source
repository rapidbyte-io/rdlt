//! The one integration binary: every offline case lives under `cases/`.
//! The two spawn suites (`e2e.rs`, `spawned_pipeline.rs`) stay their own
//! binaries because the Makefile selects them by name.

mod cases;
