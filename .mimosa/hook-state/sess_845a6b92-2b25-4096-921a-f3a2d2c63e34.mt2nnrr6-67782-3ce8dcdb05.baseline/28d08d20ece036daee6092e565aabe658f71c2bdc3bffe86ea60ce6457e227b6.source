//! Test doubles for the `serve` suites — separate from `cases::example`
//! (the framework's own conformance proof) because these exist to be
//! driven over the wire, not to certify the in-process shell.

pub mod echo;
