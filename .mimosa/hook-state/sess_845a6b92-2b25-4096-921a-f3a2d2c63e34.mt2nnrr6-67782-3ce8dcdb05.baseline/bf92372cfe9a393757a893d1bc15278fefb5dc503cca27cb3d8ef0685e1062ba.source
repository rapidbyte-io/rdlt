//! The reference source: ONE jsonl file, its stream named by the file
//! stem, resumed by a byte-offset cursor. `config` is the document,
//! `cursor` the persisted cursor and its resume law, `connector` the sdk
//! `SourceConnector`.

pub mod config;
pub mod connector;
pub(crate) mod cursor;
