//! The reference destination: jsonl parts plus commit receipts, in ONE
//! output directory. `config` is the document, `connector` the sdk
//! `DestinationConnector` and the session lease, `session` the sdk
//! `Backend` choreography, `store` the on-disk formats and the fsync
//! discipline, `part` the part-file naming.

pub mod config;
pub mod connector;
pub(crate) mod part;
pub mod session;
pub(crate) mod store;
