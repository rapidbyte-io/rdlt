//! The reference connector: the connector contract's own connector.
//!
//! ONE jsonl file in, jsonl parts plus commit receipts out — the
//! smallest complete connector the sdk can express, certified by the
//! same conformance kits and wire clauses every shipping connector
//! answers to. It exists so the engine's gates ride a connector the
//! contract itself owns (044), and so `docs/connector-authoring.md`
//! has a living companion: everything the doc requires of a connector
//! is here, and nothing else is.

pub mod destination;
pub mod source;
