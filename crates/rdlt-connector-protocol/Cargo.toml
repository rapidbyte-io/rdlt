[package]
name = "rdlt-connector-protocol"
description = "rdlt out-of-process connector wire protocol v0: the gRPC/protobuf contract and the stdout handshake line"
readme = "README.md"
# EXPERIMENTAL (ADR 0001 D8): unpublished until the conformance kit and one
# non-Rust implementation have exercised the proto.
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tonic = { workspace = true }
tonic-prost = { workspace = true }
prost = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[build-dependencies]
tonic-prost-build = { workspace = true }
protoc-bin-vendored = { workspace = true }

[lints]
workspace = true
