//! STUB — filled in by the build sequence (029 plan D4).

/// The crash points this destination arms (stub — populated with the
/// connector).
pub const FAIL_POINTS: &[&str] = &[];

/// The Iceberg destination (stub).
#[derive(Debug, Clone)]
pub struct Iceberg;

#[doc(hidden)]
pub mod testhook {}
