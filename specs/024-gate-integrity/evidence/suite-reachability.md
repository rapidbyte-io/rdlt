Every test binary in the repository, and the gate path that reaches it (GI2).
Derived mechanically: all crates/*/tests/*.rs against what each gate target selects.

CRATE                        BINARY                   REACHED BY
rdlt-bench                   selftest                 check -> test (workspace)
rdlt-cli                     build_parity             check -> test (workspace)
rdlt-connector-duckdb        classification           check -> test (workspace)
rdlt-connector-duckdb        conformance              check -> test (workspace)
rdlt-connector-duckdb        differential             check -> test (workspace)
rdlt-connector-duckdb        error_codes              check -> test (workspace)
rdlt-connector-duckdb        golden_ensure_sql        check -> test (workspace)
rdlt-connector-duckdb        json                     check -> test (workspace)
rdlt-connector-duckdb        probes                   check -> test (workspace)
rdlt-connector-duckdb        recovery                 check -> test (workspace)
rdlt-connector-duckdb        refinements              check -> test (workspace)
rdlt-connector-duckdb        strategies               check -> test (workspace)
rdlt-connector-duckdb        sweep                    check -> test TARGET=sweep
rdlt-connector-file          config_schema            check -> test (workspace)
rdlt-connector-file          conformance              check -> test (workspace)
rdlt-connector-file          csv                      check -> test (workspace)
rdlt-connector-file          dest_conformance         check -> test (workspace)
rdlt-connector-file          dest_options             check -> test (workspace)
rdlt-connector-file          e2e_copy                 check -> test TARGET=e2e   [ADDED by this feature]
rdlt-connector-file          e2e_duckdb               check -> test TARGET=e2e   [ADDED by this feature]
rdlt-connector-file          jsonl                    check -> test (workspace)
rdlt-connector-file          parquet_resume           check -> test (workspace)
rdlt-connector-file          prefix_semantics         check -> test (workspace)
rdlt-connector-file          preservation             check -> test (workspace)
rdlt-connector-file          recovery                 check -> test (workspace)
rdlt-connector-file          s3_live                  check -> test (workspace)
rdlt-connector-file          sweep                    check -> test TARGET=sweep
rdlt-connector-iceberg       auth_probe               check -> test (workspace)
rdlt-connector-iceberg       catalog_live             check -> test (workspace)
rdlt-connector-iceberg       config_schema            check -> test (workspace)
rdlt-connector-iceberg       conflict                 check -> test (workspace)
rdlt-connector-iceberg       exactly_once             check -> test (workspace)
rdlt-connector-iceberg       interop                  check -> test (workspace)
rdlt-connector-iceberg       nested_types             check -> test (workspace)
rdlt-connector-iceberg       partitioning             check -> test (workspace)
rdlt-connector-iceberg       providers                check -> test (workspace)
rdlt-connector-iceberg       spark_deep               deep only (JVM, heavyweight)
rdlt-connector-iceberg       sweep                    check -> test TARGET=sweep
rdlt-connector-postgres      cdc_crash_sweep          check -> test TARGET=sweep
rdlt-connector-postgres      cdc                      check -> test (workspace)
rdlt-connector-postgres      config_schema            check -> test (workspace)
rdlt-connector-postgres      conformance              check -> test (workspace)
rdlt-connector-postgres      crash_sweep              check -> test TARGET=sweep
rdlt-connector-postgres      dest_conformance         check -> test (workspace)
rdlt-connector-postgres      dest_crash_sweep         check -> test TARGET=sweep
rdlt-connector-postgres      dest_recovery            check -> test (workspace)
rdlt-connector-postgres      differential             check -> test (workspace)
rdlt-connector-postgres      direct_publish_guarantees check -> test (workspace)
rdlt-connector-postgres      golden_ensure_sql        check -> test (workspace)
rdlt-connector-postgres      golden_sql               check -> test (workspace)
rdlt-connector-postgres      golden_unit_sql          check -> test (workspace)
rdlt-connector-postgres      incremental              check -> test (workspace)
rdlt-connector-postgres      memory_bound             deep only (RDLT_HEAVY, hard-fails on missing prereqs)
rdlt-connector-postgres      pg_copy_wire_pin         check -> test (workspace)
rdlt-connector-postgres      query_streams            check -> test (workspace)
rdlt-connector-postgres      reflect_live             check -> test (workspace)
rdlt-connector-postgres      scd2                     check -> test (workspace)
rdlt-connector-postgres      tls_matrix               check -> test (workspace)
rdlt-connector-rest          actions                  check -> test (workspace)
rdlt-connector-rest          auth                     check -> test (workspace)
rdlt-connector-rest          children                 check -> test (workspace)
rdlt-connector-rest          config_schema            check -> test (workspace)
rdlt-connector-rest          conformance              check -> test (workspace)
rdlt-connector-rest          pagination               check -> test (workspace)
rdlt-connector-rest          pokeapi_live             check -> test (workspace)
rdlt-connector-rest          robustness               check -> test (workspace)
rdlt-connector-rest          sweep                    check -> test TARGET=sweep
rdlt-connector-snowflake     conformance              check -> test (workspace)
rdlt-connector-snowflake     crash_sweep              NOT RUN by any gate; type-checked by lint [ADDED]. 101.5 min, live creds.
rdlt-connector-snowflake     differential_oracle      check -> test (workspace)
rdlt-connector-snowflake     ingestion_session        check -> test (workspace)
rdlt-connector-snowflake     live_auth_matrix         check -> test (workspace)
rdlt-connector-snowflake     live_client              check -> test (workspace)
rdlt-connector-snowflake     live_economy             check -> test (workspace)
rdlt-connector-snowflake     live_load                check -> test (workspace)
rdlt-connector-snowflake     live_merge               check -> test (workspace)
rdlt-connector-snowflake     live_semantics           check -> test (workspace)
rdlt-connector-snowflake     options_parity           check -> test (workspace)
rdlt-connector-snowflake     quickstart_doc           check -> test (workspace)
rdlt-connector-snowflake     scratch_reclaim          check -> test (workspace)
rdlt-connector-snowflake     secret_hygiene           check -> test (workspace)
rdlt-connector-snowflake     statement_economy        check -> test (workspace)
rdlt-connector               object_safe              check -> test (workspace)
rdlt-core                    identity_props           check -> test (workspace)
rdlt-core                    lattice_laws             check -> test (workspace)
rdlt-core                    naming_props             check -> test (workspace)
rdlt-engine                  crash_sweep              check -> test TARGET=sweep
rdlt-engine                  entry_points             check -> test (workspace)
rdlt-engine                  misbehaving_source       check -> test (workspace)
rdlt-engine                  mutation_closures        check -> test (workspace)
rdlt-engine                  passthrough              check -> test (workspace)
rdlt-engine                  shred_identity_pin       check -> test (workspace)
rdlt-engine                  shred_property           deep -> test TARGET=prop  [selector FIXED by this feature]
rdlt-engine                  shred_roundtrip          check -> test (workspace)
rdlt-engine                  us1_full_sync            check -> test (workspace)
rdlt-engine                  us2_incremental          check -> test (workspace)
rdlt-engine                  us3_crash_matrix         check -> test (workspace)
rdlt-engine                  us4_policies             check -> test (workspace)
rdlt-engine                  us5_observability        check -> test (workspace)
rdlt-engine                  value_fidelity           check -> test (workspace)
rdlt-engine                  wal_lifecycle            check -> test (workspace)
rdlt-testkit                 conformance_memory       check -> test (workspace)
rdlt-testkit                 conformance_negative     check -> test (workspace)
rdlt-testkit                 gating_pin               check -> test (workspace)
rdlt-testkit                 scanner_selfcheck        check -> test (workspace)
rdlt                         builder_errors           check -> test (workspace)
rdlt                         facade                   check -> test (workspace)

TOTAL: 107 test binaries. Zero unreachable-and-unexplained.
